# СЛОВАРИ

tmp.base_ingredients_map
tmp.bodies_map
tmp.countries_map
tmp.designations_map
tmp.foods_map
tmp.glasswares_map
tmp.regions_map
tmp.scales_map
tmp.sites_map
tmp.subregions_map
tmp.tasting_notes_map
tmp.varietal_map
tmp.wineries_map


CREATE DICTIONARY IF NOT EXISTS tmp.dict_base_ingredients_map (
    name String,
    id UInt32
) PRIMARY KEY name
SOURCE(CLICKHOUSE(TABLE 'tmp.base_ingredients_map')) -- укажите реальное имя таблицы-зеркала
LAYOUT(HASHED()) LIFETIME(MIN 3000 MAX 7200);

CREATE DICTIONARY IF NOT EXISTS tmp.dict_bodies_map (
    name String,
    id UInt32
) PRIMARY KEY name
SOURCE(CLICKHOUSE(TABLE 'tmp.bodies_map')) -- укажите реальное имя таблицы-зеркала
LAYOUT(HASHED()) LIFETIME(MIN 3000 MAX 7200);

CREATE DICTIONARY IF NOT EXISTS tmp.dict_base_ingredients_map (
    name String,
    id UInt32
) PRIMARY KEY name
SOURCE(CLICKHOUSE(TABLE 'tmp.base_ingredients_map')) -- укажите реальное имя таблицы-зеркала
LAYOUT(HASHED()) LIFETIME(MIN 3000 MAX 7200);




tmp.countries_map
tmp.designations_map
tmp.foods_map
tmp.glasswares_map
tmp.regions_map
tmp.scales_map
tmp.sites_map
tmp.subregions_map
tmp.tasting_notes_map
tmp.varietal_map
tmp.wineries_map